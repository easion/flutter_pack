name: flutter_pack
description: A tool to make development & distribution of flutter-gix apps.
version: 0.1.0
repository: https://github.com/easion/flutter_pack

environment:
  sdk: ^3.0.5

executables:
  flutter_pack: flutter_pack

dependencies:
  flutter_tools:
    sdk: flutter
  package_config: ^2.1.0
  github: ^9.15.0
  file: ^6.1.4
  path: ^1.8.3
  args: ^2.4.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
